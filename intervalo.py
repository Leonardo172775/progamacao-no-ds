# Solicita ao usuário que insira um número inteiro
limite = int(input("Digite um número inteiro: "))

# Gera e exibe a sequência de números de 0 até o número fornecido
for numero in range(limite + 1):
    print(numero)
